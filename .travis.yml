language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t konman01/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true konman01/docker-react npm run test